# 📋 체크리스트: 전체 식재료 데이터 V2.1 전면 고도화 (Total Scientization)

## 🎯 목표
- 기존 79종 식재료 데이터를 새로운 V2.1 규격(과학적 속성, 열 반응, 기능적 특성 등)으로 전면 업그레이드합니다.
- 서비스 전체의 데이터 일관성(Consistency)과 전문성(Authority)을 확보합니다.

---

## 🛠️ 작업 단계

### 1. 현황 분석 및 전략 수립
- [x] 기존 8개 카테고리 파일의 V2.0 데이터 구조 전수 검토
- [x] 업그레이드 우선순위 설정 (다빈도 사용 재료 우선)

### 2. V2.1 필드 자동 생성 및 리서치
- [x] 카테고리별로 기존 데이터를 읽어 V2.1 필드를 보충하는 업그레이드 스크립트(`upgrade_to_v21.js`) 작성
- [x] AI(LLM) 및 웹 리서치를 통해 111종의 과학적 수치(pH, 수분, 점도 등) 확보
- [x] `swap_warnings` 및 `functional_properties` 필드 보강

### 3. 데이터 통합 및 검증
- [x] 업그레이드된 데이터를 각 카테고리 JSON 파일에 덮어쓰기
- [x] `schema.json`을 통한 전체 데이터 무결성 검사
- [x] UI에서 새로운 필드(속성, 열 반응 등)가 정상적으로 노출되는지 샘플 테스트

### 4. 완료 보고
- [x] `MEMORY.md` 및 `INGREDIENTS_LIST.md`에 V2.1 전면 적용 사실 기록
- [x] 최종 결과 보고

---

## ⚠️ 주의 사항
- 79종 + 신규 32종(총 111종)의 방대한 데이터이므로 작업 중 데이터 유실이 없도록 백업 필수.
- 과학적 수치는 가능한 한 신뢰할 수 있는 범위(USDA, 논문 등) 내에서 설정.
- 기존 V2.0 필드(description, substitutes 등)는 보존하면서 새로운 필드만 추가/확장.
