# 📋 체크리스트: 식재료 항목 제목 추출 (Management List)

## 🎯 목표
- `src/lib/data/ingredients/` 폴더 내의 모든 JSON 파일에서 식재료의 `id`, `name.ko`, `name.en`을 추출하여 하나의 통합 리스트(Markdown)로 생성합니다.
- 향후 데이터 관리 및 중복 확인을 위한 기준 문서를 마련합니다.

---

## 🛠️ 작업 단계

### 1. 환경 분석 및 준비
- [x] `src/lib/data/ingredients/` 폴더 내 JSON 파일 목록 확정
- [x] 데이터 스키마 확인 (`id`, `name.ko`, `name.en` 필드 존재 여부)

### 2. 데이터 추출 스크립트 실행
- [x] Node.js 스크립트를 작성하여 모든 JSON 파일을 순회하며 데이터 추출
- [x] 추출된 데이터를 카테고리별로 정렬

### 3. 결과물 생성
- [x] `.gravityBrain/INGREDIENTS_LIST.md` 파일 생성 및 기록
- [ ] (옵션) 빠른 검색을 위한 JSON 형태의 색인 파일 생성

### 4. 검증 및 마무리
- [x] 총 아이템 개수(79개 예상)와 추출된 개수 비교
- [x] `MEMORY.md`에 작업 완료 기록

---

## ⚠️ 주의 사항
- 파일 크기가 크므로 직접 `view_file`로 모든 내용을 읽기보다 스크립트를 통해 필요한 필드만 추출합니다.
- 한글/영어 이름이 누락된 항목이 있는지 체크합니다.
