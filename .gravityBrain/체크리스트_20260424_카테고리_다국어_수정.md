# 🛠️ 체크리스트: 카테고리 다국어 노출 버그 수정 (2026-04-24)

## 1. 에러 분석 (Root Cause Analysis)
- [x] **가설 1**: `IngredientExploreView.jsx`에서 카테고리를 추출할 때 영어 필드(`en`)만 사용함. (확인됨)
- [x] **가설 2**: '전체' 카테고리 버튼의 i18n 키가 `ko.json`과 일치하지 않음. (`common.all` vs `common.all_categories`) (확인됨)
- [x] **가설 3**: 검색 결과 카운트 배지의 "Items" 문자열이 하드코딩되어 있음. (확인됨)

## 2. 수정 작업 (Implementation)
- [x] `IngredientExploreView.jsx` 카테고리 추출 로직 수정 (Map 객체 활용)
- [x] `IngredientExploreView.jsx` 카테고리 칩 렌더링 다국어 대응
- [x] `src/lib/i18n/ko.json` 및 `en.json`에 `items_count` 및 `loading` 키 추가
- [x] `IngredientExploreView.jsx` 및 `src/app/explore/page.js`에 번역 키 적용

## 3. 검증 설계 (Verification)
- [x] 한국어(KO) 상태에서 카테고리 버튼이 한국어로 나오는지 확인
- [x] '전체' 버튼이 '전체'로 나오는지 확인
- [x] 영어(EN) 상태에서 카테고리 버튼이 영어로 유지되는지 확인
- [x] 카테고리 클릭 시 필터링 기능이 정상 작동하는지 확인 (ID/EN 값 기준 비교)
- [x] 결과 카운트 배지의 텍스트가 다국어에 맞게 변하는지 확인
